mcp>=1.9.0
typer>=0.12.0
rich>=13.0.0
httpx>=0.25.0
uvicorn>=0.30.0          # HTTP transport (MCP_TRANSPORT=http or sse)

# LLM providers — install at least one for security_scan_server
anthropic>=0.40.0          # provider="anthropic"
openai>=1.0.0              # provider="openai"
google-genai>=1.0.0        # provider="gemini" (new SDK)
# google-generativeai>=0.8.0  # alternative legacy Gemini SDK

# Cisco MCP Scanner — for provider="cisco"
# YARA + Readiness engines run offline with no API keys
# Set MCP_SCANNER_LLM_API_KEY for AST/behavioral analysis
# Set MCP_SCANNER_API_KEY for Cisco AI Defense ML engine
cisco-ai-mcp-scanner>=4.0.0

# Snyk agent-scan — for provider="snyk"
# Set SNYK_TOKEN for prompt-injection (E001) detection; structural checks run without it
# Zero-install alternative: uvx snyk-agent-scan@latest (no pip install needed)
snyk-agent-scan>=0.4.0

# MCPSafetyScanner 5-stage pipeline — for provider="mcpsafety+<llm>"
# Enhanced over https://github.com/johnhalloran321/mcpsafetyscanner
# (paper: arxiv 2504.03767 — MCP Safety Audit by Radosevich & Halloran)
# Auditor agent uses DuckDuckGo + HackerNews + Arxiv for CVE/paper research
# Ollama provider requires: pip install openai + a running Ollama instance
duckduckgo-search>=6.0.0
arxiv>=2.0.0               # optional — Arxiv academic paper enrichment in auditor stage
